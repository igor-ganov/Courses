/**
 * ВСЕ БЛОКИ — один импорт, который заполняет реестр.
 *
 * Порядок здесь и есть порядок в галерее блоков. Ничего, кроме импортов и
 * повторного вывоза, тут быть не должно: как только сюда попадёт логика,
 * реестр перестанет быть единственной точкой расширения.
 */

export * from './testo';
export * from './interattivi';
export * from './orchestra';
