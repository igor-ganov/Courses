/**
 * «KUBERNETES: ВВЕДЕНИЕ» — курс, ради которого написана платформа.
 *
 * Устроен так, что понятие сначала встречается в работающей модели, а
 * определение получает после. Обратный порядок — определение, потом пример —
 * даёт заученную формулировку и ноль понимания; в этом предмете особенно,
 * потому что заучить его словарь легко и совершенно бесполезно.
 *
 * Порядок модулей — порядок чтения. Порядок спирали — то, что платформа
 * предлагает на самом деле: сперва все темы на первом витке, чтобы читатель
 * встретил каждую мысль один раз прежде, чем любая из них станет трудной.
 *
 * Сквозная нить у курса одна: **всё есть цикл сверки**. Тираж, выкатка,
 * масштабирование, вытеснение — один и тот же механизм в разных костюмах, и
 * задача курса не перечислить костюмы, а научить узнавать механизм.
 */

import { defineCourse } from '~/content/model';
import { dichiarativo, riconciliazione } from './temi/stato';
import { pod, scheduler } from './temi/carico';

export const kubernetes = defineCourse({
  id: 'kubernetes',
  title: 'Kubernetes: введение',
  subtitle: 'Заявка, цикл сверки, под и узел — на работающих моделях, а не на пересказе документации',
  description:
    'Вводный курс, в котором Kubernetes разобран не как набор команд, а как одна повторяющаяся мысль: ' +
    'записанное желаемое состояние и цикл, который к нему сводит. Каждое понятие сначала встречается ' +
    'в работающей модели — поды роняются пальцем, планировщик показывает свои отсев и оценку, — ' +
    'а определение получает после. Материал идёт по спирали: сперва все темы проходятся на первом ' +
    'витке, затем те же понятия возвращаются с новым аппаратом.',
  modules: [
    {
      id: 'stato',
      title: 'Желаемое состояние',
      glyph: '↻',
      description:
        'Чем заявка отличается от команды и кто её исполняет. Здесь появляется цикл сверки — предмет, к которому курс возвращается на каждом витке.',
      topicIds: ['dichiarativo', 'riconciliazione'],
    },
    {
      id: 'carico',
      title: 'Под и узел',
      glyph: '▦',
      description:
        'Что именно цикл сверки создаёт, почему оно не чинится на месте и как получает узел. Отсюда растёт большинство недоразумений новичка — и здесь же они кончаются.',
      topicIds: ['pod', 'scheduler'],
    },
  ],
  topics: [dichiarativo, riconciliazione, pod, scheduler],
});
